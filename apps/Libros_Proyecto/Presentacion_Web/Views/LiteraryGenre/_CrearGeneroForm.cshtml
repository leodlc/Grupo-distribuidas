@model Data.GENEROLITERARIO

@using (Html.BeginForm(Model.IDGL == 0 ? "Create" : "Edit", "LiteraryGenre", FormMethod.Post, new { id = "generoForm" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h4>Género Literario</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <input type="hidden" id="IDGL" name="IDGL" value="@Model.IDGL" />

        <div class="form-group">
            <label class="control-label col-md-2" for="NOMBREGL">Nombre</label>
            <div class="col-md-10">
                <input type="text" class="form-control" id="NOMBREGL" name="NOMBREGL" value="@Model.NOMBREGL" required maxlength="100" />
                <span class="text-danger" id="nombreError"></span>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2" for="DESCRIPGL">Descripción</label>
            <div class="col-md-10">
                <textarea class="form-control" id="DESCRIPGL" name="DESCRIPGL" rows="3" maxlength="200">@Model.DESCRIPGL</textarea>
                <span class="text-danger" id="descripcionError"></span>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <div class="text-center">
                    <input type="submit" value="Guardar" class="btn btn-success" />
                </div>
            </div>
        </div>
    </div>
}



<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

@*<script>
    $(document).ready(function () {
        // Validación del campo Nombre
        $('#NOMBREGL').keypress(function (e) {
            var charCode = e.which;
            // Permite letras (mayúsculas y minúsculas) y espacios (charCode 32)
            if ((charCode >= 65 && charCode <= 90) || (charCode >= 97 && charCode <= 122) || charCode == 32) {
                return true;
            } else {
                return false;
            }
        });

        // Validación del campo Descripción
        $('#DESCRIPGL').keypress(function (e) {
            var charCode = e.which;
            // Permite letras, números, espacios, comillas y comas
            if ((charCode >= 65 && charCode <= 90) || // Letras mayúsculas
                (charCode >= 97 && charCode <= 122) || // Letras minúsculas
                (charCode >= 48 && charCode <= 57) || // Números
                charCode == 32 || // Espacio
                charCode == 34 || // Comillas dobles (")
                charCode == 44) { // Coma (,)
                return true;
            } else {
                return false;
            }
        });

        $('#generoForm').submit(function (e) {
            var nombre = $('#NOMBREGL').val();
            var descripcion = $('#DESCRIPGL').val();
            var nombreError = $('#nombreError');
            var descripcionError = $('#descripcionError');

            // Validación del campo Nombre
            var nombrePattern = /^[a-zA-Z\s]+$/;
            if (!nombrePattern.test(nombre)) {
                nombreError.text('El nombre solo puede contener letras y espacios.');
                e.preventDefault(); // Evita el envío del formulario
            } else {
                nombreError.text('');
            }

            // Validación del campo Descripción
            var descripcionPattern = /^[a-zA-Z0-9\s",]+$/;
            if (!descripcionPattern.test(descripcion)) {
                descripcionError.text('La descripción solo puede contener letras, números, espacios, comillas y comas.');
                e.preventDefault(); // Evita el envío del formulario
            } else {
                descripcionError.text('');
            }
        });
    });
</script>*@
